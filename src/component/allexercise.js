import React, {  useContext, useState } from 'react'
import { Search } from '../api/context'
const Allexercise = (props) => {
    const {search,setsearch}=useContext(Search);console.log(search)
    return (
        <div>
            
        </div>
    )
}

export default Allexercise
